# AWS Load Balancer Controller — IAM setup notes

# 0) Prereqs
# - eksctl installed
# - kubectl configured to the EKS cluster
# - OIDC provider associated with the cluster

eksctl utils associate-iam-oidc-provider \
  --region us-west-1 \
  --cluster eks-microservices \
  --approve

# 1) Download the OFFICIAL IAM policy JSON for the controller and keep it in this folder
#    (Update version if you adopt a newer controller)
curl -o alb-controller-policy.json \
  https://raw.githubusercontent.com/kubernetes-sigs/aws-load-balancer-controller/v2.13.3/docs/install/iam_policy.json

# 2) Create the customer-managed IAM policy in your account
aws iam create-policy \
  --policy-name AWSLoadBalancerControllerIAMPolicy \
  --policy-document file://alb-controller-policy.json

# 3) Create the service account + IAM role (IRSA) that uses the policy
#    Replace <ACCOUNT_ID> with your AWS account ID
eksctl create iamserviceaccount \
  --cluster eks-microservices \
  --namespace kube-system \
  --name aws-load-balancer-controller \
  --attach-policy-arn arn:aws:iam::<ACCOUNT_ID>:policy/AWSLoadBalancerControllerIAMPolicy \
  --override-existing-serviceaccounts \
  --region us-west-1 \
  --approve

# 4) Install the controller via Helm (match chart/controller version to the docs)
helm repo add eks https://aws.github.io/eks-charts
helm repo update

# NOTE: If you’re on EC2 nodes with restricted IMDS, or on Fargate,
#       you MUST set region and vpcId (and ensure the SA above is used).
helm upgrade --install aws-load-balancer-controller eks/aws-load-balancer-controller \
  -n kube-system \
  --set clusterName=eks-microservices \
  --set serviceAccount.create=false \
  --set serviceAccount.name=aws-load-balancer-controller \
  --set region=us-west-1 \
  --set vpcId=$(aws eks describe-cluster --name eks-microservices --region us-west-1 \
                 --query "cluster.resourcesVpcConfig.vpcId" --output text)

# 5) Verify deployment
kubectl -n kube-system rollout status deployment/aws-load-balancer-controller
kubectl -n kube-system get deploy aws-load-balancer-controller

# References (keep for future updates):
# - AWS EKS User Guide (Helm install + version pin): search "Install AWS Load Balancer Controller with Helm"
# - aws-load-balancer-controller GitHub repo (official iam_policy.json)
